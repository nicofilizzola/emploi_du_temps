{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="row">
            <div class="col-md-9 mx-auto alert alert-{{ type }}">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}