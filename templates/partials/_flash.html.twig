{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="row mt-5">
            <div class="col-md-6 mx-auto alert alert-{{ type }}">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}