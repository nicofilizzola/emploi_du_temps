<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Plateforme de gestion de l'emploi du temps{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" type="text/css">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fullcalendar@5.5.1/main.min.css" type="text/css">
            <style>
                body{
                    overflow-x: hidden;
                }
            </style>
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}
         
    </head>

    <body>
    {% include 'partials/nav/_nav-R.html.twig' %}
    {% include 'partials/flash.html.twig' %}
    {% block body %}{% endblock %}

    </body>

    {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js" integrity="sha256-u7e5khyithlIdTpu22PHhENmPcRdFiHRjhAuHcs05RI=" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.5.1/main.min.js" type="text/javascript"></script>
        <script>
            var events = document.querySelectorAll('.js-event-button');
            var searchbar = document.querySelector('#js-session-searchbar');
            searchbar.addEventListener('keyup', function(e){
                events.forEach(event => {
                    if (event.textContent.toLowerCase().indexOf(searchbar.value.toLowerCase()) == -1){
                        event.classList.add('d-none');
                    } else {
                        if (event.className.indexOf('d-none') !== -1){
                            event.classList.remove('d-none');
                        }
                    }
                });
            })


            /*$("#js-session-searchbar").on("keyup input", function () {
                if (this.value.length > 0) {
                    $("#js-session-events button").each(function(){
                        $(this).hide().filter(function () {
                            return $(this).text().toLowerCase().lastIndexOf($("#js-session-searchbar").val().toLowerCase(),0)==  0;
                        }).show();
                    });
                }
                else {
                 $("js-session-events button").show();
                }
            });*/
        </script>
        {#{{ encore_entry_script_tags('app') }}#}
    {% endblock %}
</html>
